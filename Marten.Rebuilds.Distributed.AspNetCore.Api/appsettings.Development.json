{
  "Logging": {
    "LogLevel": {
      "Default": "Information"
    }
  },
  "ConnectionStrings": {
    "RabbitMq": "amqp://admin:admin@localhost:5672",
    "Postgres": "host=localhost:5433;database=rebuilds;password=postgres;username=postgres",
    "Redis": "localhost:6379,password=redis"
  }

}
