{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Npgsql.Command": "Warning"
    }
  },
  "ConnectionStrings": {
    "RabbitMq": "amqp://admin:admin@localhost:5672",
    "Postgres": "host=localhost:5433;database=rebuilds;password=postgres;username=postgres;persist security info=true",
    "Redis": "localhost:6379,password=redis"
  }

}
